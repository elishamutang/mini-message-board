<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/index.css" />
  </head>
  <body>
    <main>
      <h1 class="title"><%= title %></h1>
      <div>
        <% messages.forEach((message) => { %>
        <a href="messages/<%= message.id %>" class="userDetails">
          <h2><%= message.user %></h2>
          <p>Message: <span><%= message.text %></span></p>
          <p>Date added: <span><%= message.added %></span></p>
        </a>
        <% }) %>
      </div>
      <a class="submitMsg" href="new">Submit your message!</a>
    </main>
  </body>
</html>
